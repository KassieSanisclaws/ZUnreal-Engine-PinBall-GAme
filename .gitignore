# Set the default behavior, in case users don't have core.autocrlf set.
* text=auto

# Force bash scripts to always use LF
*.sh text eol=lf

# Force Unreal Engine files to use CRLF
*.uproject text eol=crlf
*.uasset binary
*.umap binary
